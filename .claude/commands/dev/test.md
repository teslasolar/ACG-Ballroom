---
description: Run the project test suite (currently the MCP smoke test).
---

Run:

```bash
node src/mcp/test-stdio.js
```

Surface the output and report PASS/FAIL based on whether the run prints
`ALL PASS` and exits 0.
